name: Trigger Expanded Physical Examinations

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  dispatch:
    if: ${{ !contains(github.event.head_commit.message, 'Disable broken examination expansion trigger') }}
    runs-on: ubuntu-latest
    steps:
      - name: Examination expansion trigger disabled
        run: echo 'Detailed examination content is maintained directly in the repository.'
